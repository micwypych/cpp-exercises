version: '{build}'

platform:
- x64

environment:
  matrix:
  - GENERATOR: MinGW Makefiles


build:
  verbosity: detailed

install:
  - cmake --version

before_build:
  # Workaround for CMake not wanting sh.exe on PATH for MinGW
  - ECHO %PATH%
  - set PATH=C:\mingw-w64\x86_64-6.3.0-posix-seh-rt_v5-rev1
  - mkdir build
  - cd build
  - cmake -G "%GENERATOR%" ..

build_script:
  - mingw32-make lab1_double_base_palindrome_tests
  - tests/lab1test/lab1_double_base_palindrome_tests 
